import { SliceEllipsisPipe } from './slice-ellipsis.pipe';

describe('SliceEllipsisPipe', () => {
    it('create an instance', () => {
        const pipe = new SliceEllipsisPipe();
        expect(pipe).toBeTruthy();
    });
});
