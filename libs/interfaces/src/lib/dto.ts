import { BlockConfig, Project, Step, User } from './interfaces';

export interface UserDTO extends User {}
export interface ProjectDTO extends Project {}
export interface StepDTO extends Step {}
export interface BlockDTO extends BlockConfig {}
